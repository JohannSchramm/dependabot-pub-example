name: dependabot-pub-example
description: A sample Dart/Flutter project

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core: ^0.4.5 # Test Comment
  firebase_auth: ^0.16.1
  cloud_firestore: ^0.13.7
  provider: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
